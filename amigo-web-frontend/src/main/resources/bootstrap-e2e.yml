spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
      fail-fast: false

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false

feign:
  hystrix:
    enabled: false

security:
  basic:
    enabled: false
